# server.cjs — только для локального запуска (node server.cjs), не для Vercel.
# Написан в CommonJS (require), а package.json теперь "type":"module" для api/*.js —
# если Vercel попробует собрать/проанализировать server.cjs, сборка падает целиком.
server.cjs
server-out.log
server-err.log
_screenshots/
