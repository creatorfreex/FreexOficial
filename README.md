# BEST — public landing

> Единственная папка Claude, живущая как отдельный git-репозиторий и реально задеплоенная в
> интернет (`origin` → `github.com/creatorfreex/FreexOficial`). Раньше здесь жил прототип
> DeepSeek-чата — история и troubleshooting того модуля сохранены в
> `.agents/Claude/07_REPORTS/2026-07-22_ARCHIVE_deepseek-chat-public-repo-README.md`, не
> потеряны, просто эта папка теперь про другое.

**Статус:** живой публичный лендинг BEST — золотой тор + каллиграфический wordmark, canvas-
анимация, тап по слову переключает по кругу между 6 подтверждёнными направлениями:
Hit.parad.official (жёлтый) → Valmont (красный) → Animix (фиолетовый) → Aleorix (розовый) →
Jelly (тёмное золото) → BestOfficial (золото).

## Что внутри

Один файл, `index.html` — статика, без бэкенда, без API-ключей, без серверных функций.
Canvas-анимация кольца + CSS для текста, всё в одном файле намеренно (простой лендинг, не
приложение).

## Публичный адрес

Тот же Vercel-проект, что раньше показывал DeepSeek-чат — при пуше в `main` передеплоится
автоматически: **https://freex-oficial.vercel.app**

## Источник кода

Живая рабочая/экспериментальная версия с полной историей правок и внутренними комментариями —
`control/best-vision.html` в основном репозитории (`creatorfreex/freex`, приватный). Эта копия —
публичный снимок, урезанный: без панели переключения на 40+ направлений (в приватной версии
список гораздо больше, показаны только подтверждённые Создателем), без API вызовов, без
внутренних dev-комментариев. Обновляется вручную при следующем деплое, не автоматически.
