# FreexOfficial DeepSeek Chat — минимальный публичный модуль

**Статус: Прототип, проверено локально.** Отдельный, изолированный модуль — не главная страница FreexOfficial, не пульт управления. Цель: проверить подключение нового AI-агента к одному простому чату.

**Папка переименована** 2026-07-17: было `Claude/deepseek-chat-public/`, стало `Claude/Публичный репозиторий/` — это единственная папка Claude, которая является отдельным git-репозиторием (`origin` → `github.com/creatorfreex/FreexOficial`), содержимое которой предназначено для публикации. Остальные файлы Claude сюда не попадают.

## Что реализовано
- `index.html` / `styles.css` / `app.js` — интерфейс: статус подключения, история сообщений, ввод, кнопка «Новый чат», настройки, выбор модели.
- `server.js` — локальный сервер-прокси (порт **8768**). Единственная задача: держать реальный API-ключ на сервере, чтобы он не попадал в браузер/HTML/JS.
- Модели: **`deepseek-v4-flash`** (по умолчанию) и **`deepseek-v4-pro`** — актуальные названия, проверено по официальной документации DeepSeek 2026-07-17 (`api-docs.deepseek.com`). Старые `deepseek-chat`/`deepseek-reasoner` официально отключаются 24.07.2026 15:59 UTC — сознательно не использованы.
- Обработка ошибок: 401 / 402 / 429 / 500 / 503 — отдельные понятные сообщения на русском (`app.js`, `errorMessageFor()`).

## Ключ — где и как
Приоритет на сервере: **ключ из этой вкладки браузера** (введён в ⚙ Настройках, хранится только в JS-переменной `inMemoryKey`, не в localStorage/sessionStorage, исчезает при перезагрузке) → иначе **`Искусственный интеллект/SECRETS/deepseek.key`** (общее хранилище проекта, решение руководителя от 2026-07-17). Ключ никогда не возвращается клиенту и не попадает в исходный код страницы — проверено: `curl` к `/api/chat` без ключа и с тестовым фейковым ключом (401 от настоящего api.deepseek.com, не сетевая ошибка) — оба раза сервер не отдал ключ обратно.

## Как запустить локально
```
cd "Искусственный интеллект/Claude/Публичный репозиторий"
node server.js
```
Открыть `http://127.0.0.1:8768/`.

## Публикация — код готов к Vercel, деплой ещё не запущен

`api/chat.js` + `api/health.js` + `vercel.json` добавлены 2026-07-17 — тот же паттерн, что уже работает в проде у исходного проекта FreeX (`Я/История/freex/freex/api/chat.js`, задеплоен на `freex-eta.vercel.app`): статика + serverless-функция, ключ только через переменную окружения `DEEPSEEK_API_KEY` на стороне Vercel, никогда в коде.

**Не задеплоено** — на этой машине нет Vercel CLI и нет авторизации (`vercel` не установлен, `~/.local/share/com.vercel.cli` не существует). Нужно либо (а) Создатель запускает `vercel login` сам, либо (б) даёт токен Vercel — по тому же принципу, что и с GitHub-токеном (`Я/Мои секреты/`).

Отдельно, тем же способом, что и раньше: GitHub Pages сам по себе не подходит (только статика, `server.js`/`api/chat.js` там не выполнятся) — Vercel решает это лучше, т.к. сам исполняет serverless-функции.

Репозиторий `github.com/creatorfreex/FreexOficial` создан Создателем, локально закоммичено (`24033e7`, ветка `main`, `origin` настроен) — **push по-прежнему не проходит**: у токена нет права `Contents: Read and write` на репозиторий (подтверждено и Claude, и независимо Kilo Code, см. `Искусственный интеллект/START.md` → раздел «Настройки» → GitHub). Проверено повторно 2026-07-17 — та же ошибка.

## Что НЕ сделано (честно)
- Push в GitHub — всё ещё не проходит (см. выше).
- Деплой на Vercel — код готов, сам деплой не запущен (нет доступа).
- PR / CodeRabbit-ревью — не создавались (нет успешного push).
- Проверка с телефона / по мобильной сети — не делалась (модуль только на `127.0.0.1`).

## Next step
1. Vercel-доступ (логин или токен) **или** починка прав GitHub-токена — любое из двух разблокирует публикацию.
2. После доступа: `vercel --prod` из этой папки (или `git push`, если чинят токен).
3. **Известный конфликт**: `Kilo Code/10_PROTOTYPE/server.js` тоже использует порт 8768 — при параллельном локальном запуске с этим модулем возможен конфликт, см. `Искусственный интеллект/START.md`.
